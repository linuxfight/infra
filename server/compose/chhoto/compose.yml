services:
  chhoto-url:
    image: sintan1729/chhoto-url:latest
    restart: always
    container_name: chhoto-url
    ports:
      - "4567:4567"
    env_file:
      - .env
    volumes:
      - ./data/:/db
