networks:
  infra:
    external: true

services:
  xray:
    container_name: xray
    image: ghcr.io/xtls/xray-core:latest
    restart: always
    command: run -c /etc/xray/xray.json
    volumes:
      - "./xray.json:/etc/xray/xray.json:ro"
    networks:
      - infra
